# git init

## What is it?
git init creates a new Git repository in the current folder.

## Why do we need it?
Before Git can track changes, it needs to initialize the project.
This command creates a hidden `.git` folder that stores version history.

## Basic Syntax
```bash
git init
